# 修仙重生模拟器 - 项目结构说明

## 项目结构

```
game/
├── index.html              # 主HTML文件（仅包含UI结构和游戏入口）
├── main.js                 # Electron主进程配置
├── src/                    # 源代码目录
│   ├── utils.js            # 工具函数模块
│   ├── data.js             # 数据加载模块
│   ├── ui.js               # UI渲染和更新模块
│   └── game.js             # 游戏主逻辑模块
├── excel/                  # CSV数据文件
│   ├── S_玩家属性表.csv
│   ├── C_出生词条表.csv
│   └── S_事件表.csv
├── 打包应用.bat            # 打包应用脚本
├── 启动游戏.bat            # 启动游戏脚本
└── package.json            # 项目配置文件
```

## 模块说明

### 1. utils.js - 工具函数模块
**职责：** 提供通用的工具函数

**主要函数：**
- `loadCSV(path)` - 加载CSV文件并解析
- `selectWordsByWeight(words, count)` - 根据权重随机选择词条
- `selectEventByWeight(events)` - 根据权重随机选择事件
- `$(selector)` - DOM选择器快捷函数

**特点：**
- 独立性强，不依赖其他模块
- 可被其他模块复用
- 易于测试和维护

---

### 2. data.js - 数据加载模块
**职责：** 负责加载和管理游戏数据

**主要函数：**
- `loadAllData()` - 加载所有游戏数据
- `getBaseAttr()` - 获取基础属性
- `getWords()` - 获取词条数组
- `getEvents()` - 获取事件数组

**特点：**
- 封装数据加载逻辑
- 提供统一的数据访问接口
- 便于扩展新的数据源

---

### 3. ui.js - UI渲染和更新模块
**职责：** 处理所有UI相关的渲染和更新

**主要函数：**
- `renderWords(words, onWordClick)` - 渲染词条列表
- `updateUI(player)` - 更新玩家属性显示
- `log(text, content)` - 添加日志
- `updateStatus(text, color)` - 更新状态显示
- `showWordPanel()` / `hideWordPanel()` - 显示/隐藏词条面板
- `enableNextButton()` / `disableNextButton()` - 启用/禁用按钮
- `clearLog()` - 清空日志

**特点：**
- 集中管理所有UI操作
- 提供清晰的UI控制接口
- 便于UI样式调整

---

### 4. game.js - 游戏主逻辑模块
**职责：** 实现游戏核心逻辑

**主要变量：**
- `player` - 玩家对象
- `selected` - 已选词条索引数组
- `randomWords` - 随机显示的词条数组
- `alive` - 存活状态
- `baseAttr` - 基础属性
- `words` - 词条数据
- `events` - 事件数据

**主要函数：**
- `initData(attr, wordList, eventList)` - 初始化游戏数据
- `init()` - 初始化游戏
- `handleWordClick(index, element)` - 处理词条点击
- `confirmWords()` - 确认选择的词条
- `nextAge()` - 下一岁
- `trigger(event)` - 触发事件
- `checkDead()` - 检查玩家是否死亡
- `rebirth()` - 重新开始游戏

**特点：**
- 封装游戏核心逻辑
- 管理游戏状态
- 提供游戏控制接口

---

### 5. index.html - 游戏主入口
**职责：** 提供UI结构和启动游戏

**主要内容：**
- HTML结构（仅包含UI元素）
- CSS样式
- 模块引入（script标签）
- 游戏启动逻辑
- 事件绑定
- 窗口缩放适配

**特点：**
- 结构清晰，易于理解
- 职责单一，仅负责启动和布局
- 便于UI修改

---

## 优势

### 1. 模块化
- 每个模块职责明确
- 降低代码耦合度
- 提高代码复用性

### 2. 可维护性
- 修改某个功能只需找到对应文件
- 代码结构清晰，便于理解和协作
- 新增功能时不会让单个文件过于庞大

### 3. 可测试性
- 可以单独测试各个模块
- 便于定位和修复bug
- 提高代码质量

### 4. 可扩展性
- 添加新功能时只需新增或修改对应模块
- 便于集成第三方库
- 支持功能插件化

---

## 开发流程

### 修改UI样式
1. 编辑 `index.html` 中的 `<style>` 部分
2. 或者修改 `src/ui.js` 中的渲染逻辑

### 修改游戏逻辑
1. 找到 `src/game.js` 中对应的函数
2. 修改逻辑代码
3. 测试功能

### 添加新功能
1. 在相应模块中添加新函数
2. 在 `index.html` 中绑定事件
3. 测试功能

### 修改数据
1. 编辑 `excel/` 目录下的CSV文件
2. 无需修改代码（如果字段名不变）

---

## 注意事项

1. **模块加载顺序**：index.html中必须按顺序加载utils.js → data.js → ui.js → game.js
2. **全局变量**：避免在各模块中创建同名全局变量
3. **函数命名**：遵循驼峰命名法，使用有意义的名称
4. **注释规范**：添加清晰的注释说明函数用途和参数

---

## 未来优化建议

1. **使用ES6模块**：可以将各模块改为ES6模块语法（import/export）
2. **打包工具**：使用Webpack或Vite进行代码打包和压缩
3. **类型检查**：使用TypeScript进行类型检查
4. **测试框架**：添加单元测试和集成测试
5. **状态管理**：考虑引入状态管理库（如Vuex、Redux）
6. **UI框架**：考虑使用Vue或React等UI框架
