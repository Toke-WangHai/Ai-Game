# 修仙重生模拟器 - 开发环境安装说明

## 项目信息

- **项目名称**：修仙重生模拟器（xiuxian）
- **当前版本**：v1.3.3
- **技术栈**：HTML + CSS + JavaScript（原生，无框架）
- **桌面端**：Electron 28
- **打包工具**：electron-builder 26

---

## 一、安装 Node.js

项目开发时使用的环境版本：

| 工具 | 版本 |
|------|------|
| Node.js | v20.19.4 |
| npm | 10.8.2 |

**下载地址**：https://nodejs.org/

> 建议安装 Node.js 20 LTS 或更高版本。安装 Node.js 时会自动附带 npm。

安装完成后，打开命令行验证：

```bash
node -v    # 应显示 v20.x.x
npm -v     # 应显示 10.x.x
```

---

## 二、安装项目依赖

在项目根目录打开命令行，执行：

```bash
npm install
```

这会根据 `package.json` 和 `package-lock.json` 自动安装以下依赖到 `node_modules/` 目录：

| 依赖包 | 版本 | 用途 |
|--------|------|------|
| electron | ^28.0.0 | 将网页打包为桌面应用 |
| electron-builder | ^26.8.1 | 构建 Windows EXE 发布版本 |

---

## 三、常用命令

### 启动开发版（Electron 桌面窗口）

```bash
npm start
```

或者直接双击 `启动游戏.bat`。

### 导表（CSV → JS 数据文件）

修改 `excel/` 目录下的 CSV 表后，运行：

```bash
npm run export
```

或者双击 `导表工具.bat`。这会将 CSV 数据转换为 `src/data.js`。

### 创建发布版本

```bash
node build-release.js
```

或者双击 `创建发布版本.bat`。会生成：

- `dist/win-unpacked/` — EXE 电脑版（Electron 独立应用）
- `dist-pc/` — 网页电脑版（浏览器直接打开）

### Electron 打包（仅 EXE）

```bash
npm run pack
```

---

## 四、项目结构

```
Ai-Game/
├── index.html          # 游戏主页面
├── style.css           # 全局样式
├── main.js             # Electron 主进程入口
├── sw.js               # PWA Service Worker（离线缓存）
├── manifest.json       # PWA 清单文件
├── package.json        # 项目配置和依赖声明
├── package-lock.json   # 依赖版本锁定（勿删）
│
├── src/                # 游戏源码（JS 模块）
│   ├── data.js             # 游戏数据（由导表工具生成）
│   ├── data-processor.js   # 数据处理器
│   ├── game.js             # 游戏核心逻辑
│   ├── game-init.js        # 游戏初始化
│   ├── ui.js               # 主界面
│   ├── battle-system.js    # 战斗系统
│   ├── equipment.js        # 装备系统
│   ├── dungeon.js          # 秘境系统
│   ├── blessed-land.js     # 福地系统
│   ├── alchemy-system.js   # 炼丹系统
│   ├── shop-system.js      # 商店系统
│   ├── save-manager.js     # 存档管理
│   └── ...                 # 其他模块
│
├── excel/              # 游戏数据源表（CSV 格式）
│   ├── C_出生词条表.csv
│   ├── S_事件表.csv
│   ├── Z_装备表.csv
│   ├── W_物品表.csv
│   └── ...
│
├── assets/             # 游戏资源（图标、图片）
│   ├── icons/              # SVG 图标
│   ├── icon-192.png        # PWA 图标
│   └── icon-512.png        # 应用图标
│
├── export-data.js      # 导表工具脚本
├── build-release.js    # 构建发布版本脚本
├── 启动游戏.bat         # 快捷启动（npm start）
├── 导表工具.bat         # 快捷导表（npm run export）
├── 创建发布版本.bat     # 快捷打包
└── 更新公告.txt         # 版本更新记录
```

---

## 五、快速恢复开发环境（一步到位）

```bash
# 1. 进入项目目录
cd Ai-Game

# 2. 安装依赖
npm install

# 3. 启动游戏测试
npm start
```

全部完成，即可继续开发！
